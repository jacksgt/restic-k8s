apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "restic-k8s.fullname" . }}-backup
  labels:
    {{- include "restic-k8s.labels" . | nindent 4 }}
    app.kubernetes.io/component: "backup"
spec:
  schedule: {{ .Values.restic.backup.schedule | quote }}
  suspend: {{ .Values.restic.backup.suspend }}
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      labels:
        {{- include "restic-k8s.labels" . | nindent 8 }}
        app.kubernetes.io/component: "backup"
    spec:
      backoffLimit: 0 # do not retry this job
      template:
        metadata:
          {{- with .Values.podAnnotations }}
          annotations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        spec:
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          restartPolicy: Never
          serviceAccountName: {{ include "restic-k8s.fullname" . }}-controller
          automountServiceAccountToken: true
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          {{- if or .Values.extraBackupInitContainers .Values.extraInitContainers }}
          initContainers:
            {{- if .Values.extraBackupInitContainers }}
            {{- toYaml .Values.extraBackupInitContainers | nindent 12 }}
            {{- end }}
            {{- if .Values.extraInitContainers }}
            {{- toYaml .Values.extraInitContainers | nindent 12 }}
            {{- end }}
          {{- end }}
          containers:
            - name: "restic"
              command:
                - "restic-k8s"
                - "backup"
                - "--namespace={{ .Release.Namespace }}"
                - "--config-secret={{ include "restic-k8s.configSecretName" . }}"
                - "--image={{ .Values.image.repository }}:{{ .Values.image.tag }}"
                {{- if .Values.restic.backup.dryRun }}
                - "--restic-dry-run"
                {{- end }}
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              image: {{ include "restic-k8s.image" . }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              env:
                - name: POD_NAME
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.name
                - name: POD_NAMESPACE
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.namespace
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
              {{- if or .Values.extraBackupVolumeMounts .Values.extraVolumeMounts }}
              volumeMounts:
                {{- if .Values.extraBackupVolumeMounts }}
                {{- toYaml .Values.extraBackupVolumeMounts | nindent 16 }}
                {{- end }}
                {{- if .Values.extraVolumeMounts }}
                {{- toYaml .Values.extraVolumeMounts | nindent 16 }}
                {{- end }}
              {{- end }}
            {{- if .Values.extraBackupContainers }}
            {{- toYaml .Values.extraBackupContainers | nindent 12 }}
            {{- end }}
            {{- if .Values.extraContainers }}
            {{- toYaml .Values.extraContainers | nindent 12 }}
            {{- end }}
          {{- if or .Values.extraBackupVolumes .Values.extraVolumes }}
          volumes:
            {{- if .Values.extraBackupVolumes }}
            {{- toYaml .Values.extraBackupVolumes | nindent 12 }}
            {{- end }}
            {{- if .Values.extraVolumes }}
            {{- toYaml .Values.extraVolumes | nindent 12 }}
            {{- end }}
          {{- end }}
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
